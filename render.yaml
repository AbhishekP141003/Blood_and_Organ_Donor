services:
  - type: web
    name: campusblooddonor
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_ENV
        value: production
      - key: EMAIL_ADDRESS
        value: abhip141003@gmail.com
      - key: EMAIL_PASSWORD
        value: nfldldhiikvbjmgi
